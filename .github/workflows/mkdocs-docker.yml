name: deploy
on:
  push:
    branches: [ main ]
permissions:
  contents: write

jobs:
  gh-pages:
    runs-on: ubuntu-latest
    container:
      image: squidfunk/mkdocs-material:latest
    defaults:
      run:
        working-directory: mysite          # 你的 mkdocs.yml 在這裡
    steps:
      - uses: actions/checkout@v4
      - run: pip install mkdocs-awesome-pages-plugin mkdocs-exclude mkdocs-minify-plugin mkdocs-git-revision-date-localized-plugin
      - run: git config --global user.name "github-actions[bot]"
      - run: git config --global user.email "github-actions[bot]@users.noreply.github.com"
      - run: git config --global --add safe.directory "$GITHUB_WORKSPACE"
      - run: git config --global --add safe.directory "$GITHUB_WORKSPACE/mysite"
      - run: mkdocs gh-deploy --force